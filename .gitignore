# make_dummy.py / render.py が生成する確認用の動画・画像（大きいため追跡しない）
# dummy_input.mp4 だけは「サンプル動画で試す」機能から fetch するため例外的に追跡する
examples/out.mp4
examples/*_preview.png
examples/preview.png

__pycache__/
*.pyc

# render.py が自動切り抜き（mask: "auto"/"auto+brush"）のアルファをキャッシュするディレクトリ
cache/

# `npm install --no-save playwright-core` でPlaywright回帰テストを実行した際に生成される
node_modules/
